<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proyecto.hci.prototipohci">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContenidoActual" />
        <activity android:name=".ContenidoEstante" />
        <activity android:name=".ContenidoObjeto" />
        <activity android:name=".ContenidoHistorial" />
        <activity android:name=".Temperatura" />
        <activity android:name=".DietasPrincipal" />
        <activity android:name=".DietaGenerar" />
        <activity android:name=".DietaCambios" />
        <activity android:name=".DietaVista" />
        <activity android:name=".DietaDias"></activity>
    </application>

</manifest>